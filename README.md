# Student-Management-System
This is a Python Flask-based Student Management System that allows adding, editing, and deleting students. It automatically calculates grades and remarks based on entered subject marks. The app also supports attendance tracking, CSV import/export, and PDF report card generation. All data is stored in-memory and the app runs on localhost:5000.
